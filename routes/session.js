'use strict';

async function sessionRoutes(app) {
  // POST /session/start       — Phase 1
  // POST /session/end         — Phase 1
  // GET  /session/:id/export  — Phase 1
}

module.exports = sessionRoutes;
